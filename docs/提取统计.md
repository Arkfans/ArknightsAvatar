# 角色头像与差分提取统计

- 生成时间：2026-08-12 06:17:53 中国标准时间
- 数据来源：`data/unpacked/_characters_classified.json`（2026-08-09T13:38:11+00:00）、`_avatar_match.json`（2026-08-10T18:50:10+00:00）、`_avatar_extract.json`（2026-08-11T13:49:37+00:00）、`_face_head_detect.json`（2026-08-11T21:56:06+00:00）

## 1. 分类（classify）

- 角色总数 **1779**：有内容 1773，空目录 6（`avg_npc_1750_1`、`avg_npc_1751_1`、`avg_npc_1754_1`、`avg_npc_1755_1`、`avg_npc_2163_1`、`avg_npc_2164_1`）
- 底图 2026 张，差分 11570 张（另有 54 个 `alpha.png` 为 alpha 通道贴图，不计入）

## 2. 头像匹配（match）

- 候选角色 537（仅 `avg_\d+`/`char_\d+` 命名），匹配成功 **520（96.8%）**，无候选头像 17，失败 0
- 底图 666，成功定位 649（97.4%），低置信 71

## 3. 头像提取（extract，核心）

### 3.1 总体

| 项目 | ok（新提取） | skipped（已存在） | dropped（去重丢弃） | no_box（无裁切框） | failed | 成功率* |
|---|---|---|---|---|---|---|
| 底图 | 1324 | 110 | 75 | 517 | 0 | **73.5%** |
| 差分 | 8127 | 1222 | 874 | 1293 | 0 | **87.9%** |

\* 成功率 = (ok + skipped) /（总数 − dropped），即最终有产物文件的比例；dropped 为多底图相似度去重的**有意丢弃**，不计入分母。

- 差分总数 11516（= 分类 11570 − 54 个 alpha.png）
- 特殊 diff（动作/姿态偏移导致头像位置与 base 不一致，已识别并重新推导）**99** 个，其中 17 个因 face/head 置信度不足最终 no_box
- 缓存：face/head 识别 命中 3/新增 1535；base 相似度 命中 44/新增 188；diff 决策 命中 0/新增 8144

### 3.2 提取方式（有 method 记录的已提取项）

| 方式 | 底图 | 占比 | 差分 | 占比 |
|---|---|---|---|---|
| match（头像模板匹配） | 459 | 33.2% | 3201 | 39.4% |
| derive（模型推导 face/head） | 922 | 66.8% | 4926 | 60.6% |
| manual（手动指定） | 0 | 0% | 0 | 0% |

- match 置信度：n=3660 均值=0.963 中位=0.977 最小=0.805 最大=0.994
- derive 置信度：n=5848 均值=0.978 中位=0.996 最小=0.802 最大=1.000
- 未使用手动档：`data/unpacked/_avatar_manual.json` 不存在（优先级最高档当前为 0）

### 3.3 按角色系别

| 系别 | 角色 | ≥1 底图成功 | 全部底图 no_box | 底图 成功率* | 差分 成功率* |
|---|---|---|---|---|---|
| avg | 1550 | 1104 | 440 | 70.9% | 87.2% |
| char | 204 | 190 | 14 | 93.1% | 94.8% |
| npc | 10 | 8 | 2 | 80.0% | - |
| other | 15 | 15 | 0 | 95.0% | 100.0% |

### 3.4 角色级结论

- 至少 1 张底图成功：**1317 / 1779（74.0%）**
- 全部底图 no_box（该角色无任何头像产物）：**456** 个（avg 440、char 14、npc 2）
- 有差分但一张都没成功：**152** 个角色

## 4. 产物核对

- `data/export/`：**1317** 个角色目录、**11034** 个 PNG；报告引用的 10783 个文件**全部存在**
- 另有 233 个不在当前报告内的历史遗留文件（旧运行产物，如 `avg_1015_aglna2_1/1$2.png` 等，多为多底图去重规则变更前生成），不影响报告
- `data/unpacked/_diff_collage/`：**902** 张差分拼贴图（有底图成功但无 diff 或角色目录缺失的角色不生成）

## 5. face/head 识别缓存

- 共 1570 张图：face 检出 **1243（79.2%）**，head 检出 **1376（87.6%）**

## 6. 失败画像与建议

1. **主要失败 = no_box（无裁切框）**：底图 517（25.5%）、差分 1293（11.2%），failed 为 0。no_box 发生在 match 阈值不足转 derive 后、face>0.8 且 head>0.7 未达标时。
2. **集中在小角色 / avg_npc 系**：全部底图 no_box 的 456 个角色中 440 个在 avg 系（含大量 `avg_npc_*` 与较新角色）；char 系底图成功率高达 93% 以上。
3. **特殊 diff 兜底有限**：99 个特殊 diff 中 17 个最终 no_box，错误均为 `face/head confidence below thresholds`（如 `char_1505_frstar_1` 的 3~7 号差分）。
4. **可提升方向**：对 no_box 角色补 `_avatar_manual.json` 手动框（当前为空）；或下调 face/head 阈值/增加特殊 diff 的第二候选框策略；清理 233 个历史遗留导出文件。

## 附：原始 stats

```json
{
  "characters": 1779,
  "base_files": 2026,
  "base_ok": 1324,
  "base_skipped": 110,
  "base_dropped": 75,
  "base_no_box": 517,
  "base_failed": 0,
  "diff_files": 11516,
  "diff_ok": 8127,
  "diff_skipped": 1222,
  "diff_dropped": 874,
  "diff_no_box": 1293,
  "diff_failed": 0,
  "diff_special": 99,
  "detect_cache_hits": 3,
  "detect_cache_new": 1535,
  "similarity_cache_hits": 44,
  "similarity_cache_new": 188,
  "diff_match_cache_hits": 0,
  "diff_match_cache_new": 8144
}
```