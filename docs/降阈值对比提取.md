# 降阈值对比提取（两轮累计）

- 生成时间：2026-08-12 06:38:11 中国标准时间
- 范围：原报告中**底图全部 no_box** 的 456 个角色（493 底图 / 1169 差分）；两轮均独立输出目录，未改动主 `data/export`；全程离线、复用识别缓存与 match 报告
- 说明：另有 16 个“部分 no_box”角色的 24 张 no_box 底图不在本次对比范围（其角色已有可用底图）

## 1. 三轮累计对比

| 阈值（match/face/head） | 底图 ok | 差分 ok | 角色（≥1 底图 ok） |
|---|---|---|---|
| 原始 0.80 / 0.80 / 0.70 | 0 / 493 | 0 / 1169 | 0 / 456 |
| 第一轮 0.70 / 0.65 / 0.60 | **68** / 493 | **361** / 1169 | **65** / 456 |
| 第二轮 0.65 / 0.60 / 0.55 | **93** / 493 | **497** / 1169 | **88** / 456 |

第二轮新增救回（相对第一轮）：**+25 底图**（derive 24 / match 1）、**+136 差分**、**+23 角色**；两轮均无回退（第一轮 ok 项在第二轮仍 ok）。

## 2. 两轮累计效果

- 第二轮后：88 个角色已有底图，仍全部 no_box 角色 **368** 个
- 仍 no_box 底图 398 张 / 差分 662 张（dropped 各 1/10，failed 0）

| 仍失败原因（底图，阈值 0.60/0.55 下） | 数量 | 说明 |
|---|---|---|
| face 未检出 | 310 | 降阈值无效，需手动框 |
| face 低置信（≤0.60） | 70 | 需再降到 0.60 以下或手动框 |
| head 未检出 | 14 | 需手动框 |
| head 低置信（≤0.55） | 4 | 需再降阈值或手动框 |

## 3. 全局预测（若把第二轮结果合并进主 data/export）

- 底图 no_box：517 → **424**（成功率 73.5% → **78.3%**）
- 差分 no_box：1293 → **796**（成功率 87.9% → **92.5%**）

## 4. 理论可救曲线（识别缓存模拟，face&head 双条件）

| face 阈值 | head 阈值 | 可救底图（近似） |
|---|---|---|
| 0.8 | 0.7 | 0 |
| 0.7 | 0.65 | 47 |
| 0.65 | 0.6 | 75 |
| 0.6 | 0.55 | 106 |
| 0.55 | 0.5 | 118 |
| 0.5 | 0.45 | 136 |

第二轮实测 93 张 ≈ 模拟 106（其中 13 张因 match 档/derived box 无效等未救回，另有部分由 match 档补回）。再降到 0.55/0.50 预计还能多救约 20+ 张底图。

## 5. 产物

| 轮次 | 报告 | 明细 | 头像 |
|---|---|---|---|
| 第一轮 | `data/bench/lower_thr/_avatar_extract_lowthr.json` | `_compare_detail.json` | `data/bench/lower_thr/export/`（65 角色） |
| 第二轮 | `data/bench/round2/_avatar_extract_lowthr2.json` | `_compare_detail.json` | `data/bench/round2/export/`（88 角色） |
